@layout PublicLayout
@page "/registerpage"
@inject NavigationManager NavigationManager

@* <MudCard>
    <MudForm Model="user">
        <MudCardContent>
            <MudTextField @bind-Value="TextValue" Label="Standard" Variant="Variant.Text" Margin="Margin.Dense"></MudTextField>
            <MudTextField @bind-Value="TextValue" Label="Filled" Variant="Variant.Filled" Margin="Margin.Dense"></MudTextField>
            <MudTextField @bind-Value="TextValue" Label="Outlined" Variant="Variant.Outlined" Margin="Margin.Dense"></MudTextField>
        </MudCardContent>
    </MudForm>
</MudCard> *@

 <div class="flex-container">

    <div class="card-background">
        <div>
            <h1>Words</h1>
            <hr />
        </div>
        <div class="form-background">
            <h2>Register</h2>

                  <EditForm Model="registerFormModel" OnValidSubmit="HandleSubmit">
                    <DataAnnotationsValidator/>
                    <div class="mb-3">
                        <label for="username" class="form-label">User name</label>
                        <InputText class="form-control" @bind-Value="registerFormModel.Username" id="username"/>
                        <ValidationMessage For="@(() => registerFormModel.Username)" />
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">Password</label>
                    <InputText type="password" class="form-control" @bind-Value="registerFormModel.Password" id="password" />
                    <ValidationMessage For="@(() => registerFormModel.Password)" />
                    </div>
                    <div class="mb-3">
                        <label for="password2" class="form-label">Confirm password</label>
                    <InputText type="password" class="form-control" @bind-Value="registerFormModel.Password2" id="password2" />
                    <ValidationMessage For="@(() => registerFormModel.Password2)" />
                    </div>
                    <button type="submit" class="btn btn-primary">Register</button>
                </EditForm>  
        </div>

    </div>

</div> 

@code {
    RegisterFormModel registerFormModel = new RegisterFormModel { };

    void HandleSubmit()
    {
        NavigationManager.NavigateTo("wordspage");
    }
}
